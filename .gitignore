# node_modules — never commit, install with npm install
node_modules/

# Environment variables — contains Firebase API keys & admin emails, NEVER commit
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
.dev.vars

# Keep the safe example template
!.env.example

# OS junk
.DS_Store
.DS_Store?
._*
Thumbs.db
desktop.ini
